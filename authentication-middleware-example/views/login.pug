extends layout

block content
    p Log venligst ind med dit navn
    form(method="post", action="/login")
        .row
            input(type="text", name="userName", placeholder="Enter your username")
        .row
            input(type="submit", name="submit", value="Login")